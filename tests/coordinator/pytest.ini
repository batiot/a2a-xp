[pytest]
asyncio_mode = auto
markers =
    integration: marks tests that require all 3 A2A agents running (deselect with -m "not integration")
    gemini: marks tests that call the real Gemini API — requires GOOGLE_STUDIO_API_KEY (deselect with -m "not gemini")
